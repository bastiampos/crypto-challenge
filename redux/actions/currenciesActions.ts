import axios from 'axios'

const HOST = "https://data.messari.io/api"

const currenciesActions = {
    getCurrencyBySymbol: (symbolOrName: string) => {
      return (dispatch: any) => {
        
      }
    }
}
 
export default currenciesActions
